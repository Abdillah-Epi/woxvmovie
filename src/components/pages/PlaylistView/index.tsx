import OPlaylistView from "../../organisms/PlaylistView";
import TPlaylistView from "../../templates/PlaylistView";

const PlaylistView = () => {
    return (
        <TPlaylistView>
            <OPlaylistView />
        </TPlaylistView>
    );
};

export default PlaylistView;
